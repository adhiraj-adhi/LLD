package strategies;

import models.Board;
import models.Cell;
import models.Symbol;

public class MakeHardMoveStrategy implements MakeMoveStrategy {
    @Override
    public Cell makeMove(Board board, Symbol symbol) {
        return null;
    }
}
